{
  "name": "telegram-mini-chat",
  "version": "1.0.0",
  "description": "Telegram Web App for simple chat",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "./dev.sh",
    "build": "cd client && npm install && npm run build && mkdir -p ../server/public && cp -r build/* ../server/public/ && cd ../server && npm install",
    "setup": "./setup.sh"
  },
  "author": "",
  "license": "ISC"
} 